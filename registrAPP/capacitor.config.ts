import type { CapacitorConfig } from '@capacitor/cli';

const config = {
  appId: 'com.example.app',
  appName: 'registrAPP',
  webDir: 'www', // Asegúrate de que esta ruta sea correcta
  bundledWebRuntime: false,
};
export default config;